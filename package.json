{
  "name": "parquet-mvp",
  "private": true,
  "scripts": {
    "start": "bun index.html",
    "build": "del-cli -f dist && bun build index.html --minify --outdir dist",
    "prettier": "prettier -w src index.html"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/papaparse": "^5.3.15",
    "del-cli": "^6.0.0",
    "prettier": "^3.5.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.787.0",
    "hyparquet": "^1.12.1",
    "hyparquet-compressors": "^1.1.1",
    "papaparse": "^5.5.2"
  }
}
